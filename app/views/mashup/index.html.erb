<h1>Mashup</h1>

<h2> Blogs</h2>

<%= render :partial => 'entries', :locals => { :feed => @mashup[:blogs] } %>

<h2> Bookmarks</h2>

<%= render :partial => 'entries', :locals => { :feed => @mashup[:bookmarks] } %>

<h2> Microtexts</h2>

<%= render :partial => 'entries', :locals => { :feed => @mashup[:microtexts] } %>

<h2>Videos</h2>

<%= render :partial => 'entries', :locals => { :feed => @mashup[:videos] } %>

<h2> Photos</h2>

<%= render :partial => 'photo', :locals => { :photos => @mashup[:photos] } %>

