#body
	%div
		#info
			%p Mashup &eacute; um website ou uma aplicação web que usa conteúdo de mais de uma fonte para criar um novo serviço.

			%p O Mashup da Canção Nova tem o objetivo de reunir o conteúdo multimídia referente a Canção Nova que existe em diversos sites.
		
			%p Oferecendo ao público um único endereço na internet para acompanhar o que acontece na Canção Nova, com acesso as diversas mídias fornecida pelos maiores sites da internet.
		#last_up
			%p updates
		.clear	
	.columm
		%h2 Popular hoje
		- reset_cycle 
		= render :partial => 'list_tags', :locals => { :tags => @data[:last_day]} 
	.columm
		%h3 Popular h&aacute; semanas
		= render :partial => 'list_tags', :locals => { :tags => @data[:last_7_days]} 
	.columm
		- reset_cycle 
		%h4 Campe&otilde;es de acesso
		= render :partial => 'list_tags',  :locals => { :tags => @data[:all_time]}
	
	#clear

